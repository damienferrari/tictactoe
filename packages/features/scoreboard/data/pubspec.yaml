name: scoreboard_data
description: Local persistence for the scoreboard. Implements the domain interface.
publish_to: none
version: 1.0.0

environment:
  sdk: ^3.12.0

resolution: workspace

dependencies:
  freezed_annotation: ^3.0.0
  json_annotation: ^4.12.0
  riverpod: ^3.0.0
  riverpod_annotation: ^4.0.0
  scoreboard_domain:

dev_dependencies:
  build_runner: ^2.4.0
  freezed: ^3.0.0
  json_serializable: ^6.8.0
  riverpod_generator: ^4.0.0
  test: ^1.25.0
