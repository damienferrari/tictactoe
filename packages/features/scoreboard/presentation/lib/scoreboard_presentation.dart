/// The win/loss/draw tiles.
///
/// One symbol: the row the host mounts. The tile it is made of stays internal, and this
/// layer has no provider files — nothing is fed to it from outside.
library;

export 'src/widgets/scoreboard_widget.dart';
