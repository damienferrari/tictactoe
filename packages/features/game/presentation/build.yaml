# Codegen only reads the files that expect it. That is what makes the `.br` suffix load-bearing
# rather than decorative — the tool reads it.
targets:
  $default:
    builders:
      freezed:
        generate_for:
          - "**/*.br.dart"
      riverpod_generator:
        generate_for:
          - "**/*.br.dart"
